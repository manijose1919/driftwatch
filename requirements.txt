# Runtime dependencies, pinned for reproducible builds.
# To upgrade: bump a version here, reinstall, run `pytest tests -q`, then commit.
# Dev/test tools live in requirements-dev.txt.
fastapi==0.139.1
uvicorn[standard]==0.51.0
sqlalchemy==2.0.51
httpx==0.28.1
apscheduler==3.11.3
